node_modules
.pnpm-store
.turbo
dist
.next
out
coverage
.env
.env.local
.env.*.local
*.log
.DS_Store
.vercel

# Agent skills — machine-specific symlinks, restore with `npx skills experimental_install`
.claude/skills/
.agents/skills/
*.sqlite
*.sqlite-*

# Imágenes subidas a mano desde el CMS (ver ImageUploadService) — locales,
# no versionadas, cada entorno tiene las suyas.
apps/api/uploads/
*.tsbuildinfo
