{
  "framework": null,
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "pwd && ls -la && ls -la node_modules 2>&1 | head -20 && node node_modules/typescript/bin/tsc -p tsconfig.json",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "crons": [{ "path": "/api/cron/automation", "schedule": "0 14 * * *" }]
}
