{
  "framework": null,
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../../packages/shared && pnpm run build && cd ../types && pnpm run build && cd ../../apps/api && node node_modules/typescript/bin/tsc -p tsconfig.json",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "crons": [{ "path": "/api/cron/automation", "schedule": "0 14 * * *" }]
}
